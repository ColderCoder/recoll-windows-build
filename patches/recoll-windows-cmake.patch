diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 1b7522a..74fe1eb 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -365,6 +365,22 @@ set(librecoll_SOURCES
     xaposix/safeunistd.h
     )
 
+if (WIN32)
+    list(REMOVE_ITEM librecoll_SOURCES
+        utils/execmd.cpp
+        utils/netcon.cpp
+        utils/x11mon.cpp
+        utils/rclionice.cpp
+    )
+    list(APPEND librecoll_SOURCES
+        windows/execmd_w.cpp
+        windows/fnmatch.c
+        windows/wincodepages.cpp
+        windows/strptime.cpp
+        utils/powerstatus.cpp
+    )
+endif()
+
 set(recollindex_SOURCES
     index/checkindexed.cpp
     index/checkindexed.h
@@ -378,6 +394,12 @@ set(recollindex_SOURCES
     index/webqueue.cpp
     index/webqueue.h
     )
+if (WIN32)
+    list(APPEND recollindex_SOURCES
+        index/rclmonrcv_win32.cpp
+        windows/getopt.cc
+    )
+endif()
 
 set(recollq_SOURCE
     query/recollqmain.cpp
@@ -546,6 +568,10 @@ set(COMMON_INCLUDES_DIRS
     xaposix
 )
 
+if (WIN32)
+    list(APPEND COMMON_INCLUDES_DIRS windows)
+endif()
+
 ### Targets
 
 # Libraries shared by all targets. The cmake manual says include_directories() should be avoided, btw.
@@ -577,6 +603,15 @@ target_compile_definitions(librecoll PUBLIC
     HAVE_DLOPEN
     RECOLL_DATADIR="${CMAKE_INSTALL_PREFIX}/share/recoll"
 )
+if (WIN32)
+    target_compile_definitions(librecoll PUBLIC
+        RCL_MONITOR
+        __WIN32__
+        UNICODE
+        PSAPI_VERSION=1
+        ENABLE_LIBMAGIC=1
+    )
+endif()
 
 
 ### External Libraries
@@ -605,14 +640,29 @@ target_link_libraries(librecoll PUBLIC
 include_directories(${XAPIAN_INCLUDE_DIR})
 
 if (RECOLL_ENABLE_LIBMAGIC)
-    find_package(Libmagic REQUIRED)
-    target_link_libraries(librecoll PUBLIC Libmagic::Libmagic)
+    if (WIN32)
+        find_package(unofficial-libmagic CONFIG REQUIRED)
+        target_link_libraries(librecoll PUBLIC unofficial::libmagic::libmagic)
+    else()
+        find_package(Libmagic REQUIRED)
+        target_link_libraries(librecoll PUBLIC Libmagic::Libmagic)
+    endif()
 else()
     find_package(FileCommand REQUIRED)
     target_link_libraries(librecoll PUBLIC FileCommand::FILE_PROG)
 endif()
 
-if (APPLE)
+if (WIN32)
+    find_package(ZLIB REQUIRED)
+    find_path(RECOLL_ICONV_INCLUDE_DIR NAMES iconv.h REQUIRED)
+    find_library(RECOLL_ICONV_LIBRARY NAMES iconv libiconv REQUIRED)
+    target_include_directories(librecoll PUBLIC "${RECOLL_ICONV_INCLUDE_DIR}")
+    target_link_libraries(librecoll PUBLIC
+        ZLIB::ZLIB
+        "${RECOLL_ICONV_LIBRARY}"
+        Shell32 Rpcrt4 Ws2_32 User32 Shlwapi Psapi
+    )
+elseif (APPLE)
     # assuming packages are installed via homebrew.
     include_directories(/usr/local/include /opt/homebrew/include)
     # External dependencies will be copied automatically via macdeployqt
@@ -620,6 +670,17 @@ if (APPLE)
     find_package(Iconv REQUIRED) # https://cmake.org/cmake/help/latest/module/FindIconv.html
     target_link_libraries(librecoll PUBLIC ZLIB::ZLIB Iconv::Iconv)
 endif ()
+if (WIN32)
+    add_executable(rclstartw windows/rclstartw.cpp)
+    target_include_directories(rclstartw PRIVATE
+        ${PROJECT_BINARY_DIR}
+        common
+        utils
+        windows
+    )
+    target_compile_definitions(rclstartw PRIVATE BUILDING_RECOLL UNICODE __WIN32__)
+    target_link_libraries(rclstartw PRIVATE Shell32 Psapi Kernel32)
+endif()
 
 #--- recollindex ---
 
 add_executable(recollindex ${recollindex_SOURCES})
diff --git a/src/qtgui/CMakeLists.txt b/src/qtgui/CMakeLists.txt
index 558b7a0..b91a7a9 100644
--- a/src/qtgui/CMakeLists.txt
+++ b/src/qtgui/CMakeLists.txt
@@ -112,6 +112,9 @@ set(qtgui_SOURCES
     xmltosd.h
     )
 
+if (WIN32)
+    list(APPEND qtgui_SOURCES winschedtool.cpp winschedtool.h)
+endif()
 
 #--- recoll ---
 
@@ -133,6 +136,9 @@ add_executable(recoll
     recoll.qrc
 )
 
+if (WIN32)
+    target_sources(recoll PRIVATE recoll.rc)
+endif()
 set_target_properties(recoll PROPERTIES
     RUNTIME_OUTPUT_DIRECTORY ${PROJECT_BINARY_DIR}
     INSTALL_RPATH "$ORIGIN/../${CMAKE_INSTALL_LIBDIR}/recoll"

diff --git a/src/rcldb/rclabsfromtext.cpp b/src/rcldb/rclabsfromtext.cpp
--- a/src/rcldb/rclabsfromtext.cpp
+++ b/src/rcldb/rclabsfromtext.cpp
@@ -467,7 +467,7 @@ int Query::Native::abstractFromText(
               }
               );
     // Apply requested truncation
-    if (maxtotaloccs > 0 and maxtotaloccs < result.size()) {
+    if (maxtotaloccs > 0 && maxtotaloccs < result.size()) {
         result.erase(result.begin() + maxtotaloccs, result.end());
     }
     // Possibly sort by position
